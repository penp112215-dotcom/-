# 安全政策

## 支持范围

安全修复优先应用于默认分支的最新版本。个人部署者应自行保持操作系统、Python、Node.js、Nginx 和项目依赖更新。

## 报告漏洞

请优先使用 GitHub 仓库的 **Security → Report a vulnerability** 私密报告功能。不要在公开 Issue、截图或聊天记录中披露以下内容：

- API Key、GitHub Token、SSH 私钥或 Cookie；
- VPS 登录信息、数据库内容和个人投资记录；
- 可直接利用的未修复攻击步骤；
- CloudBase 或其他平台的管理凭据。

报告中请包含受影响版本、复现条件、影响范围和建议修复方式。维护者确认问题后会尽快给出处理状态；在修复发布前请保持细节私密。

## 部署者责任

本项目是自托管参考实现，不提供托管安全承诺。公开部署至少应做到：

- 密钥只保存在服务端环境或密钥管理服务中；
- 后端使用最小权限账户运行；
- Uvicorn 不直接暴露公网；
- API 入口具备身份校验、限速和请求日志；
- 数据库定期备份并限制文件权限；
- Nginx、系统服务和依赖及时更新；
- 生产配置使用自己的环境标识、域名和服务器地址。

仓库中的示例配置与占位符不是生产凭据。若误提交真实密钥，应立即在服务商处撤销并轮换；仅从 Git 历史中删除文件并不能使已经泄露的密钥重新安全。

